To compile and run this example, you will need to include the
Terracotta jmx-util jar in the classpath, for example:

javac -cp jmx-util-1.0.0-SNAPSHOT.jar -d bin  \
  src/org/terracotta/book/coordination/jmx/jdk15/CoordinatedStagesWithClusterEvents.java 

